<div class="city_index">
    <p class="cities">Cities</p>
    <p class="city_each">
      <% @cities.each do |city| %>
        <%= link_to city.city_name, city_path(city) %><br>
      <% end %>
    </p>

    <h2><%= link_to 'Add New City', new_city_path %></h2>
</div>
